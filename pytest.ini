[pytest]
# pytest 配置

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 显示详细信息
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# 标记
markers =
    unit: 单元测试
    integration: 集成测试
    performance: 性能测试
    slow: 慢速测试
